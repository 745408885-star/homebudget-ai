[pytest]
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers
markers =
    unit: isolated tests that never connect to PostgreSQL
    integration: tests that require an explicitly isolated TEST_DATABASE_URL
